[project]
name = "blockchain-engineering"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "pyipv8>=3.2.0",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["assignment_3"]
